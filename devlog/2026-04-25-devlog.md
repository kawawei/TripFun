# 開發日誌 (Devlog)
日期：2026-04-25

## 專案結構 (Project Structure)
```
.
├── backend/                # NestJS 後端核心
│   ├── src/                # 原始碼 (Trips, Upload 模組)
│   ├── uploads/            # 靜態資源上傳目錄 (已掛載 Volume)
│   └── docker/             # 後端 Docker 配置
├── mobile/                 # Flutter 行動端/Web 端
│   ├── lib/                # 業務邏輯與 UI 組件
│   ├── assets/             # 字體與靜態資源
│   └── web/                # Web 端專屬配置
├── devlog/                 # 開發日誌與問題追蹤
├── docker/                 # 全域 Docker Compose 配置 (Prod/Local)
├── env/                    # 環境變數管理
└── 需求/                    # 產品需求與系統設計文件
```

## 畫廊 UX 互動優化與問題修復 [00:08]

### 完成功能 / 修復內容
- [x] **實作畫廊完整網頁端互動支援**：
    - 修復了 Flutter 原生在 Web 環境下遮蔽滑鼠拖曳的問題。
    - 加入左右翻頁箭頭防呆設計 (`ChevronLeft` / `ChevronRight`)。
    - 點點指示器 (Page Indicators) 自獨立至 `AppBar` 範圍之外，並加上點擊直接跳頁功能。
- [x] **修復因佈局重構造成的各項問題**：
    - 修復缺少對齊括號造成的 `Can't find ')' to match '('` 編譯錯誤。
    - **Hotfix:** 修復 `StreamProvider` 資料流加上 Dio 的 `LogInterceptor` 後，每三秒印出整個畫廊圖文 JSON 物件陣列導致終端機與開發者工具被洗版癱瘓的問題，現在皆設為 `false` 靜默輪詢。

### 提交內容
- [Commit: 5da4e19] feat(mobile): add specific left/right nav arrows and clickable page indicators for foolproof web swiping
- [Commit: 45da7ce] fix(mobile): resolve unclosed parenthesis in activity detail page
- [Commit: 8e8cb67] fix(mobile): disable log interceptor to prevent terminal flooding from polling

## 身份切換 (Login/角色選擇) 系統建置 [00:22]

### 完成功能 / 修復內容
- [x] **建立基礎使用者領域模型 (`UserEntity`)**：包含 `id` 與 `name` 等欄位。
- [x] **建立認證狀態管理 (`auth_provider`)**：
    - 使用 `Riverpod` 建立 `AuthNotifier` 管理全局身份。
    - 整合 `shared_preferences` 實現登入狀態持久化。
    - 新增 `tripMembersProvider` (FutureProvider) 從後端 API 非同步撈取旅行的成員名單。
- [x] **實作簡易身份選擇頁面 (`LoginPage`) UX/UI 大改版**：
    - 全新設計：採用漸層模糊 (Glassmorphism) 的高質感設計。
    - 將靜態寫死的成員按鈕列表，改為非同步的 `DropdownButton` (選單) 動態綁定 API。
    - 實作按鈕與版面微動畫，增加質感與體驗。
- [x] **更新應用程式入口 (`main.dart`)**：改用 `ConsumerWidget` 監聽身份狀態，實現未登入跳轉 `LoginPage`、已選擇跳轉 `MainScreen` 邏輯。
- [x] **後端支援**：在 `backend/src/modules/trips/controllers/trips.controller.ts` 新增 `GET /trips/:id/members` 路由支援動態名單抓取。

### 提交內容
- [Commit: 5f990ac] feat(mobile): implement multi-user role selection and auth state management
- [Commit: 4abbce2] feat(auth): redesign login UI to glassmorphism and fetch users from backend API
- [Commit: a75de73] docs: update devlog with UI redesign and API integration

## 登入頁面 UI 視覺升級與優化 [00:25]

### 完成功能 / 修復內容
- [x] **文字視覺設計升級**：為 `TripFun` 第一標題加入 `ShaderMask` 橘紅漸層與落陰影；修改副標題為「美國專屬旅程」並細調高質感字距。
- [x] **卡片裝飾優化**：為頂部的飛機 Icon 加入獨立的光暈與留白設計，提升畫面的層次感。

### 提交內容
- [Commit: 7728e19] style(auth): enhance typography and text styles for login page
- [Commit: 80ed48a] refactor(auth): remove hardcoded fallback members, strictly enforce backend data

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 00:27
- **部署 Hash**: 80ed48a
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，狀態為 UP / Healthy。

## 實作後端資料庫成員動態管理 [00:34]

### 完成功能 / 修復內容
- [x] **資料庫架構升級**：在 PostgreSQL 的 `trips` 資料表中，新增 `members` (JSONB) 欄位，徹底取代後端硬編碼。
- [x] **動態 API 寫入/讀取**：
    - 實作 `GET /trips/:id/members` 以真實從資料庫查詢對應行程的成員資料。
    - 實作 `POST /trips/:id/members` 支援接收 JSON 負載並覆寫入資料庫，達成完整動態修改。
- [x] **伺服器更新與資料導入**：透過部署指令更新測試伺服器，並實際執行 `curl` 指令將新名單 (“魏川峻”, “魏永進”, “張銘容”, “魏秉泓”) 成功寫入資料庫。

### 提交內容
- [Commit: 493c927] feat(backend): add jsonb members column to trip and dynamic POST endpoint
- [Commit: 5567ad4] docs: record backend DB integration in devlog

## 航班機位分流機制開發 [00:51]

### 完成功能 / 修復內容
- [x] **後端航班重構**：將資料庫的航班重構為獨立的三段航程 (TPE-GUM, GUM-HNL, HNL-LAX)，並利用 Jsonb `personal_info` 欄位寫入四名家族成員各自的專屬座位表。
- [x] **前端登入身分隔離**：在 `TripDetailPage` 串聯 `authProvider` 取得 `currentUserId` 當前登入者 ID。
- [x] **動態隱私過濾綁定**：實作 `_buildFilteredPersonalInfo`，當使用者點進去「航班資訊」卡片時，程式將自動剔除不屬於自己的座位，僅呈現該使用者的獨立座位碼，達成專屬的個人化行程體驗。

### 提交內容
- [Commit: c35d384] feat(backend): add dynamic seed for personalized flight seats and temporary reset route
- [Commit: fe0acc7] feat(mobile): display personalized flight seat information by logged-in user id

## 航班與圖片映射優化 (Backend) [01:03]

### 完成功能 / 修復內容
- [x] **實作圖片映射邏輯與資料庫寫入**：
    - 將班機圖片 (UA166, UA200, UA1169) 和機場貴賓室圖片設定至 `backend/uploads`，對外配置靜態資源存取服務。
    - 修訂與重構 `trip` API 以新增詳細航班時間與客機型號 (`Boeing 737 Max 8` 等) 以及圖片欄位 (`image_urls`)，確保前後端內容一次到位。
- [x] **修復後端 TypeORM 依賴異常**：
    - 進入 Docker 容器內使用 `pnpm install` 補齊所缺少的 `@nestjs/serve-static` 與 `multer` 套件，避免 `package-lock.json` 版本混淆。
    - 成功重啟後端，完成 Schema 資料自動遷徙與型別設定。

### 提交內容
- [Commit: 0fdb378d] feat(backend): set up image urls for flights and install serve-static pnpm dependencies

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 01:05
- **部署 Hash**: 0fdb378d
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，狀態為 UP / Starting。

## 行程清單排序邏輯修復 (資料庫訂正) [01:12]

### 完成功能 / 修復內容
- [x] **修復時間順序顯示異常**：
    - 排查並確認 11:00 班機排序在 08:30 貴賓室之前的異常，起因為測試伺服器資料庫中 `sort_order` 強制權重發生衝突（1 低於 2）。
    - 進入伺服器 PostgreSQL (`tripfun-db`)，透過 SQL 重新依據真實物理時戳更新所有節點的 `sort_order` (1~7)，確保跨日線班機的顯示時序完全正確無誤。

### 提交內容
- [Commit: 86e1a8b2] docs: record flight timeline sequence bug fix and sort_order update

## 伺服器圖片無法顯示 (404) 與 Volume 掛載復原 [01:24]

### 完成功能 / 修復內容
- [x] **追查圖片 `404 Not Found` 導致 Frontend `EncodingError` 崩潰問題**：
    - 由於 `docker compose` 設定了 `tripfun-backend-uploads` 作為持久化磁碟卷以保護先前手動上傳的相片，因此剛才透過 Git Push 帶上測試伺服器的 `backend/uploads` 飛機圖片，遭到 Volume 的空間遮蔽而無法被 NestJS 存取。
    - 進入測試伺服器端，手動執行 `docker cp /opt/project/TripFun/backend/uploads/. tripfun-backend:/app/uploads/`，將所有客機圖片檔案直接推送入 Volume 中，解決了 404 問題，所有飛機與貴賓室圖片皆可正常獲取。

## 畫廊點擊全螢幕失效修復 [01:29]

### 完成功能 / 修復內容
- [x] **修復 `DecoratedBox` 遮蔽點擊事件**：
    - 排查到畫廊上方用來營造頂部與底層陰影的漸層 `DecoratedBox` 預設吸收了螢幕接觸事件 (Hit-Test)。
    - 對其包裹 `IgnorePointer` 強制讓點擊事件穿透到底層的 `PageView > GestureDetector`，成功恢復圖片放大功能。

### 提交內容
- [Commit: 89aa8e06] fix(mobile): apply IgnorePointer to hero gradient overlay to enable tap-to-zoom

## 手動上傳關島行程圖片 [01:32]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳關島機場相關圖片至測試伺服器**：
    - 將 `圖片/關島機場.jpg` 與 `圖片/關島.jpg` (第二張) 透過 `curl POST` 請求上傳至測試伺服器 API (`43.103.3.57:8087`)。
    - 更新「關島 (GUM) 轉機」的活動節點，綁定新產生的 `image_urls`，以利前端畫廊正確載入照片。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器或本地資料庫的操作]

## Flutter字型缺失警告與亂碼排查 [01:37]

### 完成功能 / 修復內容
- [x] **修正 Web 渲染 Emoji 與特殊符號引發的 Noto Font 報錯**：
    - 查明了因 `🏖️`、`▸` 等字元在 Flutter Web CanvasKit 無法與 Noto 系列字體成功映射而產生的警告。
    - 直接透過 API 針對 `關島 (GUM) 轉機` 更新其內容字串，移除無法穩定渲染的 Emoji，將其降級為標準清單符號 (`-`)，排除潛在的 Tofu (亂碼框) 畫面隱患。

### 提交內容
- [Commit: 8a9e5bd5]

## 手動上傳夏威夷行程圖文 [01:44]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳夏威夷機場相關文字與圖片至測試伺服器**：
    - 精簡化夏威夷機場 (HNL) 的介紹文案，去除複雜格式與可能引發渲染問題的文字，並透過 API 同步寫入。
    - 將 `圖片/夏威夷-1.jpg` 與 `圖片/夏威夷-2.jpg` 分別推播至伺服器的 `/upload/image` API，取得回傳。
    - 更新「夏威夷 (HNL) 轉機」的活動節點 `16cd9c18...`，綁定圖文以利其完美呈現於畫廊。

### 提交內容
- [Commit: 65ea6fd0]

## 跨時區航班分拆與洛杉磯機場圖文綁定 [01:57]

### 完成功能 / 修復內容
- [x] **實作國際換日線的時間軸梳理與分流**：
    - 將原本的 UA1169 `plane-landing` 改回 `plane` 作為夏威夷起飛的專屬事件。
    - **新增活動節點：** 新增了真正的「抵達洛杉磯國際機場 (LAX)」事件節點，並確保其 `sort_order` 設定在 UA1169起飛之後，其餘韓國行程皆往後順延。
    - 保證了 `TripDetailPage` 在分組渲染時能正確讓降落事件緊跟在起飛事件之後而不亂序。
- [x] **上傳與綁定機場圖片**：
    - 透過腳本將本地端的 `洛杉磯機場-1.jpg` 至 `洛杉磯機場-4.jpg` 批次上傳至測試伺服器。
    - 成功綁定四張照片至 LAX 抵達節點，讓畫廊可左右滑動預覽 LAX 情境。

### 提交內容
- [Commit: 5cae52f3]

## 手動上傳洛杉磯機場介紹文案 [02:02]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳洛杉磯國際機場 (LAX) 相關文案至測試伺服器**：
    - 將使用者提供的「洛杉磯國際機場（LAX）完全攻略」文案重新排版並大幅精簡化，同時去除所有 `#`, `*`, `-` 等 Markdown 格式符號以避免畫廊內文渲染異常。
    - 透過 `curl POST` 請求成功上傳並綁定至測試伺服器 API (`43.103.3.57:8087`) 上的「抵達洛杉磯國際機場 (LAX)」事件節點。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增洛杉磯機場租車行程節點 [02:11]

### 完成功能 / 修復內容
- [x] **建立 Hertz 租車行程資訊**：
    - 將 Priceline 提供的租車確認信件精簡、爬梳並重點整理，轉換為清晰易讀的純文字格式包含取車地點與費用。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `07:00 (4/26)` 的 `TRANSPORT` 類別行程。
    - 將預約確認碼、訂單號碼、駕駛人與客服電話等結構成額外的 `personal_info` JSON 以利前端呈現於快速資訊面板。
- [x] **上傳與綁定租車圖片**：
    - 批次將位於本地的 `Hertz租車.jpg` 以及附帶的 `Minivan 廂型休旅車` 等三張圖片上傳至測試伺服器的 `/upload/image` API。
    - 將回傳的 URL 陣列綁定至剛剛建立的行程節點 `image_urls`，並且確保第一張圖片為 `Hertz租車.jpg` 以供畫廊首圖使用。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增洛杉磯中央市場行程節點 [02:18]

### 完成功能 / 修復內容
- [x] **建立洛杉磯中央市場資訊**：
    - 將使用者提供的「洛杉磯中央市場」超過百年歷史傳統美食市集介紹，進行大幅濃縮精簡並重新排版。
    - 拿掉了所有可能引發前端錯誤的 Markdown 符號改為 Emoji 來裝飾。
    - 透過 `curl POST /trips/:id/activities` 以 `sort_order: 9` (緊接在租車之後) 建立一筆時間為 `08:30 (4/26)` 的 `FOOD` 類別行程。
- [x] **上傳與綁定市場美食圖片**：
    - 將使用者準備好的 7 張 `中央市場-N.jpg/jpeg` 圖片批次上傳至測試伺服器 `/upload/image` API。
    - 將其回傳的網址綁定至剛建立好的中央市場活動節點的 `image_urls`，讓前端畫廊完整呈現美食陣列。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增天使鐵路 (Angels Flight) 行程節點 [02:20]

### 完成功能 / 修復內容
- [x] **建立天使鐵路行程資訊**：
    - 從使用者提供的長篇文章中提煉精華，捨去 Markdown 標籤以避開渲染警告，改以 Emoji 和簡潔的純文字說明鐵路歷史、《樂來越愛你》電影取景以及票價等資訊。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `10:30 (4/26)` 的 `ATTRACTION` 景點行程。
- [x] **上傳與綁定天使鐵路圖片**：
    - 將本地準備好的 4 張 `天使鐵路-N.jpg` 圖片上傳至伺服器。
    - 將上傳獲得的 URL 綁定寫入至 `image_urls`，完成畫廊首頁視覺更新。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 行程時間微調與重修 [02:22]

### 完成功能 / 修復內容
- [x] **微調中央市場與天使鐵路行程時程**：
    - 因應使用者的新規劃，透過 `curl POST` 將「洛杉磯中央市場」的開始時間由 `08:30` 提早至 `08:00`。
    - 將「漫步天使鐵路」的時間從 `10:30` 提早至 `09:00`，讓兩者時間更加緊湊連貫。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增華特迪士尼音樂廳行程節點 [02:28]

### 完成功能 / 修復內容
- [x] **建立音樂廳行程資訊**：
    - 將長篇文章經過去蕪存菁轉換成順暢的散步路線介紹。排版上延續了不使用 Markdown 特殊符號改用 Emoji 的作風。
    - 內容涵蓋免費參觀時段、現場買低價演奏會門票秘訣以及各種網美打卡點介紹。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `10:00 (4/26)` 的 `ATTRACTION` 景點行程。
- [x] **上傳與綁定音樂廳圖片**：
    - 將準備好的 3 張 `華特迪士尼音樂廳-N.jpeg` 圖片上傳至伺服器。
    - 將獲得的 URL 陣列綁定寫入至新建節點的 `image_urls`，完美呈現音樂廳的建築之美。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增 Thriller MV 拍攝聖地行程節點 [02:35]

### 完成功能 / 修復內容
- [x] **建立 Thriller 拍攝地行程資訊**：
    - 將地點 3701 Union Pacific Ave, Los Angeles 包裝為麥可傑克森傳世經典 MV 的朝聖點。
    - 內容維持全文字加 Emoji 的乾淨排版模式。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `11:30 (4/26)` 的 `ATTRACTION` 景點行程。
- [x] **上傳與綁定朝聖點圖片**：
    - 將本地 `Thriller-1.jpg` 與 `Thriller-2.jpg` 兩張圖片上傳至測試伺服器 `/upload/image` API。
    - 將伺服器回傳之 URL 陣列綁定至新建活動節點的 `image_urls`，讓歌迷能透過畫廊感受歷史氣息。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增 Alhambra Costco 行程節點 [02:37]

### 完成功能 / 修復內容
- [x] **建立 Costco 採買購物資訊與圖片**：
    - 將地址 2207 W Commonwealth Ave, Alhambra 設定為公路旅行與接下來行程的物資補給站。
    - 加入經典 1.5 美元熱狗午餐、大份量瓶裝水、零食餅乾等推薦清單，排版上依然使用 Emoji 來優化視覺體驗。
    - 透過 `curl POST /trips/:id/activities` 以 `SHOPPING` 類型並配戴 `shopping-cart` 圖示新增一筆時間為 `12:00 (4/26)` 的行程節點。
    - 上傳 `costco.jpg` 並綁定至該節點。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增世界第一間麥當勞博物館行程節點 [02:38]

### 完成功能 / 修復內容
- [x] **建立第一間麥當勞博物館資訊**：
    - 將地址 1398 N E St, San Bernardino 設定為行程景點。
    - 內容涵蓋麥當勞兄弟創立史、Ray Kroc 的事業版圖、以及參觀重點（吉祥物、15美分漢堡看板等）。
    - 透過 `curl POST /trips/:id/activities` 以 `ATTRACTION` 類型並配戴 `museum` 圖示新增一筆時間為 `14:00 (4/26)` 的行程節點。
- [x] **上傳與綁定朝聖點圖片**：
    - 上傳「第一間麥當勞.jpg」等 5 張系列圖片並綁定。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增拉斯維加斯抵達行程節點 [02:46]

### 完成功能 / 修復內容
- [x] **建立拉斯維加斯系列行程**：
    - **Hertz 還車 (17:30)**：地點為哈里·瑞德國際機場 (LAS) 租車中心。
    - **Flamingo Hotel Check-in (18:00)**：經典紅鶴飯店，位於大道精華中心。上傳並綁定 4 張飯店實拍圖片。
    - **Michael Jackson ONE (18:20)**：Mandalay Bay 太陽馬戲團經典秀。上傳並綁定 3 張表演宣傳圖片。
- [x] **統一排版規範**：
    - 全程維持「無 Markdown 特殊符號」的純文字排版加上生動 Emoji。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 更新 MJ ONE 細節與新增舊城區 (Fremont Street) 行程 [02:51]

### 完成功能 / 修復內容
- [x] **深化 Michael Jackson ONE 秀場資訊**：
    - 根據最新的細節更新內容，加入 5,415 個揚聲器、表演特效與入場提醒等深度介紹，優化觀賞體驗。
- [x] **建立拉斯維加斯舊城區 (Fremont Street) 行程**：
    - **抵達時間 (20:45)**：介紹全世界最大的 LED 天幕燈光秀、拉霸機滑索與老建築風華。
    - **停留至 (23:00)**：隨後返回飯店整理行李。
    - **上傳並綁定 3 張舊城區圖片**：讓前端呈現閃耀霓虹的舊城視覺。
- [x] **規畫跨夜大峽谷啟程 (00:00, 4/27)**：
    - 正式新增 4/27 凌晨出發的公路旅行節點，作為旅程的新起點。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 全面修復內文排版與補齊租車圖片 [02:54]

### 完成功能 / 修復內容
- [x] **修正全域內文換行格式**：
    - 針對拉斯維加斯所有新節點（Hertz、Flamingo、MJ ONE、Fremont、大峽谷出發），透過 API 注入正確的換行符號，解決文字擠壓問題。
- [x] **補齊拉斯維加斯還車圖片**：
    - 在「Hertz 還車 (LAS)」節點補上 `Hertz租車.jpg` 視覺。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## Git 提交記錄 [02:54]
- **提交時間**: 02:54
- **提交 Hash**: `508549d7`
- **提交訊息**: `docs: finalize devlog with project structure and troubleshooting`
    - 補完今日所有行程操作之完整開發日誌紀錄。
    - 同步更新專案目錄結構（依要求排除圖片目錄）。
    - 於 `troubleshooting.md` 紀錄 Flutter Web 內文換行失效之排查與解決方案。
- **變更統計**: 37 個檔案 (+30/-0)

## 修正 Git 追蹤範圍與排查紀錄 [02:57]

### 完成功能 / 修復內容
- [x] **修正 Git 追蹤範圍**：
    - 將包含二進制圖檔的 `圖片/` 目錄加入 `.gitignore`。
    - 執行 `git rm -r --cached 圖片/` 從 Git 索引中移除已追蹤的圖檔，確保其只存於本地。
- [x] **更新問題紀錄**：
    - 將此 Git 誤操作事件記錄於 `troubleshooting.md`。

### 提交內容
- [Commit: 2bb7e81e] fix(git): remove binary image directory from version control and update gitignore

## 更新紅鶴飯店 (Flamingo) 預約詳細資訊 [03:01]

### 完成功能 / 修復內容
- [x] **更新飯店預約核心資訊與深度介紹**：
    - 將拉斯維加斯弗拉明戈飯店 (Flamingo Las Vegas) 的佔位資訊更新為完整的預約細節。
    - 內容涵蓋：訂單編號 `1715250141`、入住時間 `16:00 (4/26)`、退房時間 `11:00 (4/29)`。
    - 注入詳細費用明細 (總額 `USD 303.23`)、房型 (2張大床) 以及付款方式。
    - **新增飯店介紹**：整合了網頁搜尋之飯店歷史（巴格西·西格爾創立）、紅鶴棲息地、地理優勢及復古牛排館等深度介紹資訊。
- [x] **同步更新 `personal_info` 結構**：
    - 將關鍵資訊（訂單號、日期、電話、金額）寫入 JSONB 欄位，供前端快速資訊面板讀取。
- [x] **新增退房行程節點**：
    - 於 4/29 11:00 新增「飯店辦理退房」活動，完善住宿完整週期循環。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 實作活動詳情語音導覽功能 [03:04]

### 完成功能 / 修復內容
- [x] **實作 AI 語音導覽 (Audio Guide)**：
    - 於 `ActivityDetailPage` 整合 `flutter_tts` 套件。
    - 建立男女聲雙按鈕控制面板，支援即時切換與停止。
    - **智慧朗讀過濾**：設定自動識別「亮點介紹」區塊，避免朗讀繁瑣的訂單編號資訊。
- [x] **強化生命週期管理**：
    - 實作 `WidgetsBindingObserver` 監聽 App 狀態。
    - 確保當 App 縮至背景 (Paused)、暫停 (Inactive) 或離開頁面 (Dispose) 時，語音會立即切換至停止狀態，防止背景資源佔用。
- [x] **UI 視覺優化**：
    - 為按鈕加入 `AnimatedContainer` 以提供平滑的顏色與縮放跳轉回饋。
    - 播放中顯示微型載入指示器 (Progress Indicator)，提升互動感。

### 提交內容
- [Commit: 1c7bd7a0] feat(mobile): implement AI voice guide with male/female selection and lifecycle management

## 語音播放器優化與 Web 穩定性修補 [03:40]

### 完成功能 / 修復內容
- [x] **實作 Web 端語音同步序列鎖 (Sequential Sync Lock)**：
    - 解決了 Flutter Web 因並發呼叫 `speak` 導致長文本中斷的 `SpeechSynthesisErrorEvent` 問題。
    - 使用 `Completer` 強制等待單一句子完成播報後才啟動下一句，確保朗讀完整性。
- [x] **極致音質與語法過濾**：
    - **1.2x 語速加速**：將預設語速調升至 1.2 倍，聽感更為敏捷專業。
    - **Emoji 自動過濾**：引入 Regex 引擎在播放前自動清洗所有 Emoji 圖示（如 📍, 🏨），避免語音引擎讀出圖示名稱。
- [x] **UI/UX 面板升級：播放器模式**：
    - 移除男女聲選擇，改為現代化的「播放/暫停」與「重播」控制鍵。
    - **斷點續讀功能**：暫停後再次點擊播放，會精確從上次停止的句子繼續，而非從頭開始。
- [x] **HomePage UI 佈局修復**：
    - 修正了首頁行程卡片在 Web 端出現的 1px 垂直溢出問題。
- [x] **全域相容性修復**：
    - 修復了 `translator_page.dart` 的 API 呼叫衝突，確保翻譯功能的語音維持正常運作。

### 提交內容
- [Commit: 3f5b409] fix(mobile): resolve Web TTS error events and home page layout overflow
- [Commit: fc46197] fix(mobile): ensure full sequential TTS playback on Web using sync lock
- [Commit: b47700b] fix(mobile): add missing dart:async import for TTS sequential playback
- [Commit: 73c0705] feat(mobile): upgrade TTS panel to player mode with play/pause/replay and 1.2x speed
- [Commit: 83e1215] fix(mobile): restore TTS speak compatibility for translator page
- [Commit: 70fb3c3] fix(mobile): filter out emojis from TTS narration for better voice quality

## 行李清單後端化與個人化開發 [03:45]

### 完成功能 / 修復內容
- [x] **去硬編碼與資料庫遷移**：
    - 建立 `packing_items` 與 `user_packing_status` 資料表。
    - 實作後端自動初始化邏輯，將預設清單導入資料庫。
- [x] **個人化核取機制**：
    - 透過 `userId` 聯集查詢，實現「清單內容相同，但每人狀態獨立」的需求。
- [x] **補齊 CRUD API**：
    - 支援前端新增、刪除、修改行李項目，並即時同步至雲端。
- [x] **前端 Provider 重構**：
    - `PackingListNotifier` 銜接 API 服務，支援樂觀更新 (Optimistic UI)。

### 提交內容
- [Commit: 3f521ff4] feat(packing): implement backend-driven personalized packing list with CRUD
- [Commit: 6d8fd1a0] fix(backend): solve startup crash by delaying default items initialization

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功 (已修復啟動崩潰問題)
- **部署時間**: 2026-04-25 03:55
- **部署 Hash**: 6d8fd1a0
- **驗證狀態**: 所有服務均已進入 `(healthy)` 或 `Up` 狀態。

---

## 行李清單後端串接與修復 [04:34]

### 完成功能 / 修復內容
- [x] **重寫 `packing_list_page.dart`**：
    - 完全移除硬編碼靜態清單，改由後端 API 驅動。
    - 加入下拉刷新 (`RefreshIndicator`)、整體進度卡片、分類進度條。
    - 新增項目 Dialog 依後端現有分類動態生成選項。
- [x] **修復 TypeORM Entity 未注冊 (500 Error)**：
    - `PackingItem` 與 `UserPackingStatus` 補入 `app.module.ts` 的 `entities` 陣列。
- [x] **移除 Auto-Seed 邏輯**：
    - 移除 `PackingService.onModuleInit()` 自動填入預設清單邏輯，改為完全由使用者手動新增。
- [x] **修正 App baseUrl**：
    - `trip_service.dart` 的 `baseUrl` 從 `localhost:9001`（本地後端）改為 `43.103.3.57:8087`（測試伺服器），確保 App 串接正確的資料來源。
- [x] **修復載入/空狀態邏輯錯誤**：
    - 新增 `PackingState` 類別（含 `items` 與 `isLoading`），讓 UI 能正確區分「載入中」與「真正的空清單」。
    - 修正頁面 `else if (isLoading)` 與 `_showAddItemDialog` 的 `.items.map()` 語法錯誤。

### 提交內容 (Commit: 52ae4281)

## 行李清單手動化與個人化邏輯精進 [04:41]

### 完成功能 / 修復內容
- [x] **行李清單完全手動化**：
    - 修改後端 `PackingService`，移除查詢「系統預設」的功能，確保清單內容完全由使用者針對特定行程（`trip_id`）手動新增。
    - 修正 `getPackingList` 查詢條件，支援精確過濾行程相關項目。
- [x] **確認個人化勾選狀態**：
    - 驗證 `user_packing_status` 運作邏輯：基於 `userId` 獨立紀錄每個人的核取進度，確保多位旅客併行時資料互不衝突。

### 提交內容 (Commit: b6d4b52e)
- `feat(packing): enforce manual-only packing list and refine per-trip user status query`


## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 04:42
- **部署 Hash**: b6d4b52e
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，Backend 啟動成功。

## 行李清單為空修復與預設清單導入 [05:04]

### 完成功能 / 修復內容
- [x] **手動導入預設清單項目**：
    - 針對測試伺服器數據庫注入 8 個分類及其預設項目（重要文件、電子產品、衣物、盥洗用品、藥物、雨具、飛機上用品、其他）。
- [x] **修復後端過濾邏輯漏洞**：
    - 修正 `PackingService.getPackingList` 查詢條件，讓「全域預設項目 (trip_id IS NULL)」能與「行程專屬項目」同時顯示。
- [x] **完成服務器部署**：
    - 已同步代碼並更新測試伺服器（43.103.3.57:8087）後端服務。

### 提交內容 (Commit: f4eb133a)
- `fix(packing): include global items in packing list`

## 新增美國/韓國專屬行李清單 [05:08]

### 完成功能 / 修復內容
- [x] **依據需求新增專屬重要文件**：
    - 由於行程包含美國（關島轉機、夏威夷轉機、美國本土）及韓國，透過 API 手動為該行程 (TripID `44444...`) 加入以下專屬行李項目：
        - ESTA (美國電子旅遊授權)
        - K-ETA (韓國電子旅行許可)
        - 電子機票行程單(含關島夏威夷)
        - 訂房確認紀錄
        - 租車預約確認單(Hertz)
        - 國際駕照與台灣駕照正本
        - 海外旅平險保單(英文版)
        - 美金與韓幣現鈔
        - 充電頭、充電線、行動電源、耳機、筆電
        - 牙膏、刮鬍刀
        - 胃藥、感冒藥、蛋白粉、生理食鹽水
        - 輕便雨衣
        - 眼罩、便攜式腳踏吊床
        - 口罩、墨鏡、耳機發射器、水壺、現金、信用卡、金融卡、隱形眼鏡、隱形眼鏡盒
    - 將此相關資料納入「重要文件」、「其他」、「電子產品」、「盥洗用品」、「藥物」、「雨具」及「飛機上用品」分類中，並綁定為該行程 (`trip_id`) 的專屬項目。

### 提交內容
- [Commit: 97c8dc9b] docs(packing): add manual packing items (ESTA, K-ETA, etc.) to devlog for US/Korea trip

## 初次發布與打包 Android 應用程式 [05:30]

### 完成功能 / 修復內容
- [x] **配置 Android 打包資訊與圖示**：
    - 修改 `AndroidManifest.xml` 將 `android:label` 設為 `TripFun`。
    - 導入 `flutter_launcher_icons` 套件並透過 `flutter pub run flutter_launcher_icons` 產生自定義的 Android 應用圖標。
- [x] **處理 Android 平台專屬編譯錯誤**：
    - 移除不相容於行動端的 `dart:js` 引用。
    - 將老舊被廢棄的時區套件 `flutter_native_timezone` 替換為 `flutter_timezone` 並修改依賴呼叫方式，解決 AGP 8 命名空間與 JVM Target 相容性問題。
- [x] **修訂打包工作流與 APK 產出**：
    - 更新專案中的 `打包Android應用程式.md` 腳本以符合 TripFun 路徑與命名結構。
    - 成功執行 `flutter build apk --release`，產出 `TripFun-1.0.0.apk`。
    - 已透過 `adb install` 自動將發布版安裝至連接之設備。
- [x] **修正 Android 自適應圖示 (Adaptive Icons) 壓扁問題**：
    - 排查出原始圖片為 `1920x1080` 十六比九長方形，導致系統生成正方形遮罩時發生嚴重的水平擠壓。
    - 透過 macOS sips 終端機指令將其完美置中裁切成 `1080x1080` 正方形 (`android_square.png`)。
    - 在 `pubspec.yaml` 中明確設定 `adaptive_icon_background: "#06b6d4"` 與 `adaptive_icon_foreground: "android_square.png"`。
    - 重新編譯打包並更新至手機安裝，徹底解決圖案變形問題。

## 首頁 UI 調整 [05:40]

### 完成功能 / 修復內容
- [x] **新增快速切換使用者機制**：
    - 將首頁 (`HomePage`) 右上角原有的搜尋與設定 Icon 移除。
    - 改為單一的切換身份圖示 (`LucideIcons.logOut`)。
    - 串接 `authProvider.notifier.logout()` 以便點擊後能即時清空狀態，跳轉回「選擇身份」頁面 (`LoginPage`)。

### 提交內容
- [Commit: 209d6157] feat(mobile): integrate android adaptive icon and change home top right action to logout

## Android App 更新打包發布 (v1.0.1+2) [05:45]

### 完成功能 / 修復內容
- [x] **版本號躍升**：修改 `pubspec.yaml`，將版本號從 `1.0.0+1` 升級為 `1.0.1+2`。
- [x] **依賴解析與重新編譯**：執行 Flutter 最新 Release 佈局編譯，產出新版 APK (`TripFun-1.0.1.apk`)。
- [x] **發布安裝**：已成功透過 adb install 部署安裝至連接裝置。

### 提交內容
- [Commit: 1e26e618] chore(mobile): bump version to 1.0.1+2 and rebuild apk

## Android 網路權限與打包發布 (v1.0.2+3) [05:49]

### 完成功能 / 修復內容
- [x] **修復 Release 打包斷網問題**：於 AndroidManifest 補齊網際網路權限，並允許 Cleartext HTTP 流量，解決了無法連線測試伺服器的問題。
- [x] **版本號躍升**：修改 `pubspec.yaml`，版本號升級為 `1.0.2+3`。
- [x] **發布安裝**：已完成重新打包 `TripFun-1.0.2.apk` 並部署至裝置。

### 提交內容
- [無對應 Commit，待開發者後續提交]
