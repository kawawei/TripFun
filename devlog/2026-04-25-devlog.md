# 開發日誌 (Devlog)
日期：2026-04-25

## 畫廊 UX 互動優化與問題修復 [00:08]

### 完成功能 / 修復內容
- [x] **實作畫廊完整網頁端互動支援**：
    - 修復了 Flutter 原生在 Web 環境下遮蔽滑鼠拖曳的問題。
    - 加入左右翻頁箭頭防呆設計 (`ChevronLeft` / `ChevronRight`)。
    - 點點指示器 (Page Indicators) 自獨立至 `AppBar` 範圍之外，並加上點擊直接跳頁功能。
- [x] **修復因佈局重構造成的各項問題**：
    - 修復缺少對齊括號造成的 `Can't find ')' to match '('` 編譯錯誤。
    - **Hotfix:** 修復 `StreamProvider` 資料流加上 Dio 的 `LogInterceptor` 後，每三秒印出整個畫廊圖文 JSON 物件陣列導致終端機與開發者工具被洗版癱瘓的問題，現在皆設為 `false` 靜默輪詢。

### 提交內容
- [Commit: 5da4e19] feat(mobile): add specific left/right nav arrows and clickable page indicators for foolproof web swiping
- [Commit: 45da7ce] fix(mobile): resolve unclosed parenthesis in activity detail page
- [Commit: 8e8cb67] fix(mobile): disable log interceptor to prevent terminal flooding from polling

## 身份切換 (Login/角色選擇) 系統建置 [00:22]

### 完成功能 / 修復內容
- [x] **建立基礎使用者領域模型 (`UserEntity`)**：包含 `id` 與 `name` 等欄位。
- [x] **建立認證狀態管理 (`auth_provider`)**：
    - 使用 `Riverpod` 建立 `AuthNotifier` 管理全局身份。
    - 整合 `shared_preferences` 實現登入狀態持久化。
    - 新增 `tripMembersProvider` (FutureProvider) 從後端 API 非同步撈取旅行的成員名單。
- [x] **實作簡易身份選擇頁面 (`LoginPage`) UX/UI 大改版**：
    - 全新設計：採用漸層模糊 (Glassmorphism) 的高質感設計。
    - 將靜態寫死的成員按鈕列表，改為非同步的 `DropdownButton` (選單) 動態綁定 API。
    - 實作按鈕與版面微動畫，增加質感與體驗。
- [x] **更新應用程式入口 (`main.dart`)**：改用 `ConsumerWidget` 監聽身份狀態，實現未登入跳轉 `LoginPage`、已選擇跳轉 `MainScreen` 邏輯。
- [x] **後端支援**：在 `backend/src/modules/trips/controllers/trips.controller.ts` 新增 `GET /trips/:id/members` 路由支援動態名單抓取。

### 提交內容
- [Commit: 5f990ac] feat(mobile): implement multi-user role selection and auth state management
- [Commit: 4abbce2] feat(auth): redesign login UI to glassmorphism and fetch users from backend API
- [Commit: a75de73] docs: update devlog with UI redesign and API integration

## 登入頁面 UI 視覺升級與優化 [00:25]

### 完成功能 / 修復內容
- [x] **文字視覺設計升級**：為 `TripFun` 第一標題加入 `ShaderMask` 橘紅漸層與落陰影；修改副標題為「美國專屬旅程」並細調高質感字距。
- [x] **卡片裝飾優化**：為頂部的飛機 Icon 加入獨立的光暈與留白設計，提升畫面的層次感。

### 提交內容
- [Commit: 7728e19] style(auth): enhance typography and text styles for login page
- [Commit: 80ed48a] refactor(auth): remove hardcoded fallback members, strictly enforce backend data

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 00:27
- **部署 Hash**: 80ed48a
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，狀態為 UP / Healthy。

## 實作後端資料庫成員動態管理 [00:34]

### 完成功能 / 修復內容
- [x] **資料庫架構升級**：在 PostgreSQL 的 `trips` 資料表中，新增 `members` (JSONB) 欄位，徹底取代後端硬編碼。
- [x] **動態 API 寫入/讀取**：
    - 實作 `GET /trips/:id/members` 以真實從資料庫查詢對應行程的成員資料。
    - 實作 `POST /trips/:id/members` 支援接收 JSON 負載並覆寫入資料庫，達成完整動態修改。
- [x] **伺服器更新與資料導入**：透過部署指令更新測試伺服器，並實際執行 `curl` 指令將新名單 (“魏川峻”, “魏永進”, “張銘容”, “魏秉泓”) 成功寫入資料庫。

### 提交內容
- [Commit: 493c927] feat(backend): add jsonb members column to trip and dynamic POST endpoint
- [Commit: 5567ad4] docs: record backend DB integration in devlog

## 航班機位分流機制開發 [00:51]

### 完成功能 / 修復內容
- [x] **後端航班重構**：將資料庫的航班重構為獨立的三段航程 (TPE-GUM, GUM-HNL, HNL-LAX)，並利用 Jsonb `personal_info` 欄位寫入四名家族成員各自的專屬座位表。
- [x] **前端登入身分隔離**：在 `TripDetailPage` 串聯 `authProvider` 取得 `currentUserId` 當前登入者 ID。
- [x] **動態隱私過濾綁定**：實作 `_buildFilteredPersonalInfo`，當使用者點進去「航班資訊」卡片時，程式將自動剔除不屬於自己的座位，僅呈現該使用者的獨立座位碼，達成專屬的個人化行程體驗。

### 提交內容
- [Commit: c35d384] feat(backend): add dynamic seed for personalized flight seats and temporary reset route
- [Commit: fe0acc7] feat(mobile): display personalized flight seat information by logged-in user id
