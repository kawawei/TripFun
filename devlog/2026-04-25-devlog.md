# 開發日誌 (Devlog)
日期：2026-04-25

## 畫廊 UX 互動優化與問題修復 [00:08]

### 完成功能 / 修復內容
- [x] **實作畫廊完整網頁端互動支援**：
    - 修復了 Flutter 原生在 Web 環境下遮蔽滑鼠拖曳的問題。
    - 加入左右翻頁箭頭防呆設計 (`ChevronLeft` / `ChevronRight`)。
    - 點點指示器 (Page Indicators) 自獨立至 `AppBar` 範圍之外，並加上點擊直接跳頁功能。
- [x] **修復因佈局重構造成的各項問題**：
    - 修復缺少對齊括號造成的 `Can't find ')' to match '('` 編譯錯誤。
    - **Hotfix:** 修復 `StreamProvider` 資料流加上 Dio 的 `LogInterceptor` 後，每三秒印出整個畫廊圖文 JSON 物件陣列導致終端機與開發者工具被洗版癱瘓的問題，現在皆設為 `false` 靜默輪詢。

### 提交內容
- [Commit: 5da4e19] feat(mobile): add specific left/right nav arrows and clickable page indicators for foolproof web swiping
- [Commit: 45da7ce] fix(mobile): resolve unclosed parenthesis in activity detail page
- [Commit: 8e8cb67] fix(mobile): disable log interceptor to prevent terminal flooding from polling

## 身份切換 (Login/角色選擇) 系統建置 [00:22]

### 完成功能 / 修復內容
- [x] **建立基礎使用者領域模型 (`UserEntity`)**：包含 `id` 與 `name` 等欄位。
- [x] **建立認證狀態管理 (`auth_provider`)**：
    - 使用 `Riverpod` 建立 `AuthNotifier` 管理全局身份。
    - 整合 `shared_preferences` 實現登入狀態持久化。
    - 新增 `tripMembersProvider` (FutureProvider) 從後端 API 非同步撈取旅行的成員名單。
- [x] **實作簡易身份選擇頁面 (`LoginPage`) UX/UI 大改版**：
    - 全新設計：採用漸層模糊 (Glassmorphism) 的高質感設計。
    - 將靜態寫死的成員按鈕列表，改為非同步的 `DropdownButton` (選單) 動態綁定 API。
    - 實作按鈕與版面微動畫，增加質感與體驗。
- [x] **更新應用程式入口 (`main.dart`)**：改用 `ConsumerWidget` 監聽身份狀態，實現未登入跳轉 `LoginPage`、已選擇跳轉 `MainScreen` 邏輯。
- [x] **後端支援**：在 `backend/src/modules/trips/controllers/trips.controller.ts` 新增 `GET /trips/:id/members` 路由支援動態名單抓取。

### 提交內容
- [Commit: 5f990ac] feat(mobile): implement multi-user role selection and auth state management
- [Commit: 4abbce2] feat(auth): redesign login UI to glassmorphism and fetch users from backend API
- [Commit: a75de73] docs: update devlog with UI redesign and API integration

## 登入頁面 UI 視覺升級與優化 [00:25]

### 完成功能 / 修復內容
- [x] **文字視覺設計升級**：為 `TripFun` 第一標題加入 `ShaderMask` 橘紅漸層與落陰影；修改副標題為「美國專屬旅程」並細調高質感字距。
- [x] **卡片裝飾優化**：為頂部的飛機 Icon 加入獨立的光暈與留白設計，提升畫面的層次感。

### 提交內容
- [Commit: 7728e19] style(auth): enhance typography and text styles for login page
- [Commit: 80ed48a] refactor(auth): remove hardcoded fallback members, strictly enforce backend data

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 00:27
- **部署 Hash**: 80ed48a
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，狀態為 UP / Healthy。

## 實作後端資料庫成員動態管理 [00:34]

### 完成功能 / 修復內容
- [x] **資料庫架構升級**：在 PostgreSQL 的 `trips` 資料表中，新增 `members` (JSONB) 欄位，徹底取代後端硬編碼。
- [x] **動態 API 寫入/讀取**：
    - 實作 `GET /trips/:id/members` 以真實從資料庫查詢對應行程的成員資料。
    - 實作 `POST /trips/:id/members` 支援接收 JSON 負載並覆寫入資料庫，達成完整動態修改。
- [x] **伺服器更新與資料導入**：透過部署指令更新測試伺服器，並實際執行 `curl` 指令將新名單 (“魏川峻”, “魏永進”, “張銘容”, “魏秉泓”) 成功寫入資料庫。

### 提交內容
- [Commit: 493c927] feat(backend): add jsonb members column to trip and dynamic POST endpoint
- [Commit: 5567ad4] docs: record backend DB integration in devlog

## 航班機位分流機制開發 [00:51]

### 完成功能 / 修復內容
- [x] **後端航班重構**：將資料庫的航班重構為獨立的三段航程 (TPE-GUM, GUM-HNL, HNL-LAX)，並利用 Jsonb `personal_info` 欄位寫入四名家族成員各自的專屬座位表。
- [x] **前端登入身分隔離**：在 `TripDetailPage` 串聯 `authProvider` 取得 `currentUserId` 當前登入者 ID。
- [x] **動態隱私過濾綁定**：實作 `_buildFilteredPersonalInfo`，當使用者點進去「航班資訊」卡片時，程式將自動剔除不屬於自己的座位，僅呈現該使用者的獨立座位碼，達成專屬的個人化行程體驗。

### 提交內容
- [Commit: c35d384] feat(backend): add dynamic seed for personalized flight seats and temporary reset route
- [Commit: fe0acc7] feat(mobile): display personalized flight seat information by logged-in user id

## 航班與圖片映射優化 (Backend) [01:03]

### 完成功能 / 修復內容
- [x] **實作圖片映射邏輯與資料庫寫入**：
    - 將班機圖片 (UA166, UA200, UA1169) 和機場貴賓室圖片設定至 `backend/uploads`，對外配置靜態資源存取服務。
    - 修訂與重構 `trip` API 以新增詳細航班時間與客機型號 (`Boeing 737 Max 8` 等) 以及圖片欄位 (`image_urls`)，確保前後端內容一次到位。
- [x] **修復後端 TypeORM 依賴異常**：
    - 進入 Docker 容器內使用 `pnpm install` 補齊所缺少的 `@nestjs/serve-static` 與 `multer` 套件，避免 `package-lock.json` 版本混淆。
    - 成功重啟後端，完成 Schema 資料自動遷徙與型別設定。

### 提交內容
- [Commit: 0fdb378d] feat(backend): set up image urls for flights and install serve-static pnpm dependencies

## 測試伺服器部署紀錄 (Backend)
- **部署狀態**: 成功
- **部署時間**: 2026-04-25 01:05
- **部署 Hash**: 0fdb378d
- **驗證狀態**: 所有服務 (Backend, DB, Redis) 均運作中，狀態為 UP / Starting。

## 行程清單排序邏輯修復 (資料庫訂正) [01:12]

### 完成功能 / 修復內容
- [x] **修復時間順序顯示異常**：
    - 排查並確認 11:00 班機排序在 08:30 貴賓室之前的異常，起因為測試伺服器資料庫中 `sort_order` 強制權重發生衝突（1 低於 2）。
    - 進入伺服器 PostgreSQL (`tripfun-db`)，透過 SQL 重新依據真實物理時戳更新所有節點的 `sort_order` (1~7)，確保跨日線班機的顯示時序完全正確無誤。

### 提交內容
- [Commit: 86e1a8b2] docs: record flight timeline sequence bug fix and sort_order update

## 伺服器圖片無法顯示 (404) 與 Volume 掛載復原 [01:24]

### 完成功能 / 修復內容
- [x] **追查圖片 `404 Not Found` 導致 Frontend `EncodingError` 崩潰問題**：
    - 由於 `docker compose` 設定了 `tripfun-backend-uploads` 作為持久化磁碟卷以保護先前手動上傳的相片，因此剛才透過 Git Push 帶上測試伺服器的 `backend/uploads` 飛機圖片，遭到 Volume 的空間遮蔽而無法被 NestJS 存取。
    - 進入測試伺服器端，手動執行 `docker cp /opt/project/TripFun/backend/uploads/. tripfun-backend:/app/uploads/`，將所有客機圖片檔案直接推送入 Volume 中，解決了 404 問題，所有飛機與貴賓室圖片皆可正常獲取。

## 畫廊點擊全螢幕失效修復 [01:29]

### 完成功能 / 修復內容
- [x] **修復 `DecoratedBox` 遮蔽點擊事件**：
    - 排查到畫廊上方用來營造頂部與底層陰影的漸層 `DecoratedBox` 預設吸收了螢幕接觸事件 (Hit-Test)。
    - 對其包裹 `IgnorePointer` 強制讓點擊事件穿透到底層的 `PageView > GestureDetector`，成功恢復圖片放大功能。

### 提交內容
- [Commit: 89aa8e06] fix(mobile): apply IgnorePointer to hero gradient overlay to enable tap-to-zoom

## 手動上傳關島行程圖片 [01:32]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳關島機場相關圖片至測試伺服器**：
    - 將 `圖片/關島機場.jpg` 與 `圖片/關島.jpg` (第二張) 透過 `curl POST` 請求上傳至測試伺服器 API (`43.103.3.57:8087`)。
    - 更新「關島 (GUM) 轉機」的活動節點，綁定新產生的 `image_urls`，以利前端畫廊正確載入照片。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器或本地資料庫的操作]

## Flutter字型缺失警告與亂碼排查 [01:37]

### 完成功能 / 修復內容
- [x] **修正 Web 渲染 Emoji 與特殊符號引發的 Noto Font 報錯**：
    - 查明了因 `🏖️`、`▸` 等字元在 Flutter Web CanvasKit 無法與 Noto 系列字體成功映射而產生的警告。
    - 直接透過 API 針對 `關島 (GUM) 轉機` 更新其內容字串，移除無法穩定渲染的 Emoji，將其降級為標準清單符號 (`-`)，排除潛在的 Tofu (亂碼框) 畫面隱患。

### 提交內容
- [Commit: 8a9e5bd5]

## 手動上傳夏威夷行程圖文 [01:44]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳夏威夷機場相關文字與圖片至測試伺服器**：
    - 精簡化夏威夷機場 (HNL) 的介紹文案，去除複雜格式與可能引發渲染問題的文字，並透過 API 同步寫入。
    - 將 `圖片/夏威夷-1.jpg` 與 `圖片/夏威夷-2.jpg` 分別推播至伺服器的 `/upload/image` API，取得回傳。
    - 更新「夏威夷 (HNL) 轉機」的活動節點 `16cd9c18...`，綁定圖文以利其完美呈現於畫廊。

### 提交內容
- [Commit: 65ea6fd0]

## 跨時區航班分拆與洛杉磯機場圖文綁定 [01:57]

### 完成功能 / 修復內容
- [x] **實作國際換日線的時間軸梳理與分流**：
    - 將原本的 UA1169 `plane-landing` 改回 `plane` 作為夏威夷起飛的專屬事件。
    - **新增活動節點：** 新增了真正的「抵達洛杉磯國際機場 (LAX)」事件節點，並確保其 `sort_order` 設定在 UA1169起飛之後，其餘韓國行程皆往後順延。
    - 保證了 `TripDetailPage` 在分組渲染時能正確讓降落事件緊跟在起飛事件之後而不亂序。
- [x] **上傳與綁定機場圖片**：
    - 透過腳本將本地端的 `洛杉磯機場-1.jpg` 至 `洛杉磯機場-4.jpg` 批次上傳至測試伺服器。
    - 成功綁定四張照片至 LAX 抵達節點，讓畫廊可左右滑動預覽 LAX 情境。

### 提交內容
- [Commit: 5cae52f3]

## 手動上傳洛杉磯機場介紹文案 [02:02]

### 完成功能 / 修復內容
- [x] **透過 curl 上傳洛杉磯國際機場 (LAX) 相關文案至測試伺服器**：
    - 將使用者提供的「洛杉磯國際機場（LAX）完全攻略」文案重新排版並大幅精簡化，同時去除所有 `#`, `*`, `-` 等 Markdown 格式符號以避免畫廊內文渲染異常。
    - 透過 `curl POST` 請求成功上傳並綁定至測試伺服器 API (`43.103.3.57:8087`) 上的「抵達洛杉磯國際機場 (LAX)」事件節點。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增洛杉磯機場租車行程節點 [02:11]

### 完成功能 / 修復內容
- [x] **建立 Hertz 租車行程資訊**：
    - 將 Priceline 提供的租車確認信件精簡、爬梳並重點整理，轉換為清晰易讀的純文字格式包含取車地點與費用。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `07:00 (4/26)` 的 `TRANSPORT` 類別行程。
    - 將預約確認碼、訂單號碼、駕駛人與客服電話等結構成額外的 `personal_info` JSON 以利前端呈現於快速資訊面板。
- [x] **上傳與綁定租車圖片**：
    - 批次將位於本地的 `Hertz租車.jpg` 以及附帶的 `Minivan 廂型休旅車` 等三張圖片上傳至測試伺服器的 `/upload/image` API。
    - 將回傳的 URL 陣列綁定至剛剛建立的行程節點 `image_urls`，並且確保第一張圖片為 `Hertz租車.jpg` 以供畫廊首圖使用。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增洛杉磯中央市場行程節點 [02:18]

### 完成功能 / 修復內容
- [x] **建立洛杉磯中央市場資訊**：
    - 將使用者提供的「洛杉磯中央市場」超過百年歷史傳統美食市集介紹，進行大幅濃縮精簡並重新排版。
    - 拿掉了所有可能引發前端錯誤的 Markdown 符號改為 Emoji 來裝飾。
    - 透過 `curl POST /trips/:id/activities` 以 `sort_order: 9` (緊接在租車之後) 建立一筆時間為 `08:30 (4/26)` 的 `FOOD` 類別行程。
- [x] **上傳與綁定市場美食圖片**：
    - 將使用者準備好的 7 張 `中央市場-N.jpg/jpeg` 圖片批次上傳至測試伺服器 `/upload/image` API。
    - 將其回傳的網址綁定至剛建立好的中央市場活動節點的 `image_urls`，讓前端畫廊完整呈現美食陣列。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增天使鐵路 (Angels Flight) 行程節點 [02:20]

### 完成功能 / 修復內容
- [x] **建立天使鐵路行程資訊**：
    - 從使用者提供的長篇文章中提煉精華，捨去 Markdown 標籤以避開渲染警告，改以 Emoji 和簡潔的純文字說明鐵路歷史、《樂來越愛你》電影取景以及票價等資訊。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `10:30 (4/26)` 的 `ATTRACTION` 景點行程。
- [x] **上傳與綁定天使鐵路圖片**：
    - 將本地準備好的 4 張 `天使鐵路-N.jpg` 圖片上傳至伺服器。
    - 將上傳獲得的 URL 綁定寫入至 `image_urls`，完成畫廊首頁視覺更新。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 行程時間微調與重修 [02:22]

### 完成功能 / 修復內容
- [x] **微調中央市場與天使鐵路行程時程**：
    - 因應使用者的新規劃，透過 `curl POST` 將「洛杉磯中央市場」的開始時間由 `08:30` 提早至 `08:00`。
    - 將「漫步天使鐵路」的時間從 `10:30` 提早至 `09:00`，讓兩者時間更加緊湊連貫。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]

## 新增華特迪士尼音樂廳行程節點 [02:28]

### 完成功能 / 修復內容
- [x] **建立音樂廳行程資訊**：
    - 將長篇文章經過去蕪存菁轉換成順暢的散步路線介紹。排版上延續了不使用 Markdown 特殊符號改用 Emoji 的作風。
    - 內容涵蓋免費參觀時段、現場買低價演奏會門票秘訣以及各種網美打卡點介紹。
    - 透過 `curl POST /trips/:id/activities` 新增一筆時間為 `10:00 (4/26)` 的 `ATTRACTION` 景點行程。
- [x] **上傳與綁定音樂廳圖片**：
    - 將準備好的 3 張 `華特迪士尼音樂廳-N.jpeg` 圖片上傳至伺服器。
    - 將獲得的 URL 陣列綁定寫入至新建節點的 `image_urls`，完美呈現音樂廳的建築之美。

### 提交內容
- [無對應 Commit，此為直接對測試伺服器 API 的操作]
