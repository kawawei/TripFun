# 開發日誌 (Development Log) - 2026-04-24

## 任務進度 (Progress)

### 1. Git 效能優化與修復 [07:30]
- [x] **問題排查**：
    - 解決 `git status` 極度緩慢（10秒+）及 `.git/index.lock` 持續衝突問題。
    - 發現因 `git rm -r --cached` 操作異常中斷導致索引損壞及 staged delete 變更異常。
- [x] **修復執行**：
    - 終止 `fsmonitor--daemon` 殭屍程序。
    - 清理 `.git/index` 並執行 `git read-tree HEAD` 重建。
    - 恢復正常運行速度（0.02秒）。

### 2. 後端架構升級：TypeORM 與資料庫整合 [07:32]
- [x] **實體模型建立 (Entities)**：
    - 建立 `Trip` 與 `Activity` TypeORM 實體。
    - 定義多對一關聯（活動屬於行程）。
- [x] **服務層重構 (Repository Pattern)**：
    - 將 `TripsService` 從硬編碼模擬數據改為使用 `TypeORM Repository` 進行資料庫操作。
    - 預留 `PostgreSQL` 排程同步基礎。

### 3. 行動端 UI 穩定化 [07:32]
- [x] **數據模型同步**：
    - 更新 `TripDTO` 以對接後端最新的實體結構。
- [x] **首頁重構**：
    - 重新調整 `HomePage` 的數據流，確保從整合後的 NestJS API 獲取即時數據。


## 專案結構 (Project Structure)
```
backend/
├── src/
│   ├── modules/
│   │   ├── trips/
│   │   │   ├── entities/
│   │   │   │   ├── activity.entity.ts
│   │   │   │   └── trip.entity.ts
│   │   │   ├── controllers/
│   │   │   │   └── trips.controller.ts
│   │   │   ├── services/
│   │   │   │   └── trips.service.ts
│   │   │   └── trips.module.ts
│   │   └── auth/
│   └── main.ts
mobile/
├── lib/
│   ├── data/
│   │   ├── dtos/
│   │   │   ├── trip_dto.dart
│   │   │   └── activity_dto.dart
│   ├── presentation/
│   │   ├── pages/
│   │   │   └── home/
│   │   │       └── home_page.dart (Refactored)
│   │   └── providers/
│   │       └── trip_provider.dart
```

## Git 提交記錄
- **提交時間**: 07:32
- **提交 Hash**: `18e1a2c`
- **提交訊息**: `feat(backend): TypeORM + PostgreSQL integration, Trip/Activity Entity, TripsService Repository pattern`
- **變更統計**: 8 個檔案

- **提交時間**: 07:32
- **提交 Hash**: `8b919bb`
- **提交訊息**: `feat(mobile): TripDTO update, HomePage refactoring, devlog update`
- **變更統計**: 5 個檔案
