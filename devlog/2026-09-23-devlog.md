# 2026-09-23 開發日誌

## 任務清單 (Task List)
- [x] 連接 43.103.3.57 伺服器並診斷 TripFun 專案現狀 [15:22]
- [x] 評估伺服器映像檔狀態與保留必要性 (確認無須保留) [15:24]
- [x] 將伺服器 PostgreSQL 資料庫、上傳檔案與環境配置完整備份至本地 [15:24]
- [x] 精確清理 43 伺服器上的 TripFun 容器、數據卷、虛擬網路與專案檔案 [15:26]
- [x] 驗證清理後狀態並確認伺服器其他專案容器運行正常 [15:27]

## 專案結構 (Project Structure)
```
TripFun/
├── backup/                 # [NEW] 本地備份目錄 (已加入 .gitignore)
│   ├── tripfun_db_backup.sql          # 完整 PostgreSQL 資料庫導出檔 (44KB)
│   ├── tripfun_uploads_backup.tar.gz  # 用戶上傳靜態圖片資源壓縮包 (26MB)
│   └── tripfun_prod.env               # 伺服器端環境變數配置 (1KB)
├── mobile/                 # Flutter 移動端應用
├── backend/                # NestJS 後端服務
├── docker/                 # 容器化配置
└── devlog/                 # 開發日誌與問題追蹤
```

## 開發進度 (Progress)

### 1. 43 伺服器現況診斷與映像檔評估 [15:22]
- **伺服器連線**：使用 `-o PreferredAuthentications=password -o PubkeyAuthentication=no -o IdentitiesOnly=yes` 成功連接 `43.103.3.57`。
- **映像檔評估**：
  - TripFun 目前在伺服器上並無專屬的自建映像檔。
  - 所依賴之 `postgres:16-alpine` 與 `redis:7-alpine` 為官方公共基礎映像檔，目前仍被伺服器上其他專案（如力衡、鴻記、茂盛等）共同使用中。
  - 結論：**無須且不應保留/刪除共用映像檔**，未來重新部署時 Docker 會自動下載並構建。

### 2. 資料庫與上傳資源本地備份 [15:24]
- 建立本地 `backup/` 目錄並寫入 `.gitignore`，確保敏感數據不入版控。
- 透過 `pg_dump` 完整匯出 PostgreSQL 中的所有資料表與業務數據（包含 `activities`, `expenses`, `packing_items`, `trips`, `user_packing_status` 等）。
- 將伺服器 Docker 數據卷 `tripfun_tripfun-backend-uploads` 內的 70+ 個上傳檔案打包為 `tripfun_uploads_backup.tar.gz` (26MB)。
- 將伺服器 `env/prod/.env` 下載為 `tripfun_prod.env`，完整保留連線與金鑰配置。

### 3. 伺服器端資源清理與安全驗證 [15:26]
- **容器清理**：已強制停止並移除 `tripfun-db`、`tripfun-redis` 及相關容器。
- **數據卷清理**：已移除 `tripfun_tripfun-db-data` 與 `tripfun_tripfun-backend-uploads`。
- **網路清理**：已移除 `tripfun_tripfun-network`。
- **程式碼目錄**：已徹底刪除 `/opt/project/TripFun`。
- **交叉驗收**：驗證伺服器上其他多個業務專案（`weilaiacademy-*`, `case-auto-reply-bot-*`, `caas-*` 等）均持續健康運行，無受任何影響。

## Git 提交記錄

### 提交內容 (Commit: d1a10a44)
- **變更統計**：8 個檔案 (+327/-3)
- **提交時間**：15:30
- **提交訊息**：`chore: backup server data to local and clean up test server resources`
  - 新增本地備份目錄忽略規則至 `.gitignore`，確保備份數據安全不入版控
  - 記錄 43 測試伺服器資料庫與上傳檔案完整備份至本地 `backup/` 目錄
  - 記錄 43 伺服器 TripFun 容器、數據卷、網路與專案目錄之清理
  - 更新移動端 Android 16KB sqlite3 支援與原生包打包設定
